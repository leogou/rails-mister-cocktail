<h2>List of every Cocktails!</h2>

<ul>
  <% @cocktails.each do |cocktail| %>
    <li><%= link_to cocktail.name, cocktail_path(cocktail) %></li>
  <% end %>
</ul>

<div>
  <i class="fas fa-plus"></i><%= link_to 'Add a new cocktail', new_cocktail_path %>
</div>
